---
/**
 * VillagePortalModal — Fullscreen portal to the soothing Yorimichi village world
 * (https://yorimichi-world.vercel.app/).
 *
 * Immersive viewer with Japanese-inspired glassmorphism chrome,
 * 'Open in New Tab ↗' quick link, Fullscreen toggle, and audio/resource
 * pausing upon close.
 */
---

<div
  class="village-portal-backdrop"
  id="village-portal-backdrop"
  aria-hidden="true"
  role="dialog"
  aria-modal="true"
  aria-label="Yorimichi Soothing Village World Portal"
>
  <div class="village-portal-stage">
    <iframe
      id="village-portal-iframe"
      class="village-portal-iframe"
      src="about:blank"
      loading="eager"
      allow="autoplay; fullscreen; accelerometer; gyroscope; gamepad"
      referrerpolicy="no-referrer"
      title="Yorimichi — Soothing Village World"
    ></iframe>

    <!-- Top floating chrome -->
    <div class="village-portal-chrome">
      <div class="village-portal-badge">
        <span class="village-emblem">⛩️</span>
        <div class="village-title-group">
          <div class="village-title-main">
            <span class="village-kanji">寄り道</span>
            <span class="village-name">YORIMICHI</span>
            <span class="village-tag">SOOTHING VILLAGE WORLD</span>
          </div>
          <p class="village-subtitle">Peaceful stylized countryside exploration experience</p>
        </div>
      </div>

      <div class="village-portal-actions">
        <a
          href="https://yorimichi-world.vercel.app/"
          target="_blank"
          rel="noopener noreferrer"
          class="village-action-btn open-tab-btn"
          title="Open standalone world in a new browser tab"
        >
          <span>Open in New Tab</span>
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"></path>
            <polyline points="15 3 21 3 21 9"></polyline>
            <line x1="10" y1="14" x2="21" y2="3"></line>
          </svg>
        </a>

        <button
          type="button"
          class="village-action-btn fullscreen-btn"
          id="village-fullscreen-btn"
          title="Toggle Fullscreen"
          aria-label="Toggle Fullscreen"
        >
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="15 3 21 3 21 9"></polyline>
            <polyline points="9 21 3 21 3 15"></polyline>
            <line x1="21" y1="3" x2="14" y2="10"></line>
            <line x1="3" y1="21" x2="10" y2="14"></line>
          </svg>
        </button>

        <button
          type="button"
          class="village-action-btn close-btn"
          id="village-portal-close"
          aria-label="Return to Spot World"
          title="Return to Spot World (ESC)"
        >
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
            <line x1="18" y1="6" x2="6" y2="18"></line>
            <line x1="6" y1="6" x2="18" y2="18"></line>
          </svg>
          <span class="close-label">Return to City</span>
        </button>
      </div>
    </div>
  </div>
</div>

<style>
  .village-portal-backdrop {
    position: fixed;
    inset: 0;
    z-index: 500;
    background: #040810;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.32s cubic-bezier(0.16, 1, 0.3, 1), visibility 0.32s ease;
  }

  .village-portal-backdrop.open {
    opacity: 1;
    visibility: visible;
  }

  .village-portal-stage {
    position: relative;
    width: 100%;
    height: 100%;
    background: #020408;
    overflow: hidden;
  }

  .village-portal-iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: 0;
    display: block;
    background: #020408;
  }

  .village-portal-chrome {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 16px;
    padding: 14px 20px;
    pointer-events: none;
    background: linear-gradient(
      to bottom,
      rgba(2, 6, 14, 0.88) 0%,
      rgba(2, 6, 14, 0.6) 45%,
      transparent 100%
    );
    z-index: 10;
  }

  .village-portal-badge {
    pointer-events: auto;
    display: flex;
    align-items: center;
    gap: 12px;
    background: rgba(10, 15, 26, 0.72);
    border: 1px solid rgba(244, 63, 94, 0.3);
    border-radius: 12px;
    padding: 8px 16px;
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.5), 0 0 20px rgba(244, 63, 94, 0.15);
  }

  .village-emblem {
    font-size: 1.4rem;
    line-height: 1;
    filter: drop-shadow(0 2px 8px rgba(244, 63, 94, 0.4));
  }

  .village-title-group {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .village-title-main {
    display: flex;
    align-items: center;
    gap: 8px;
    line-height: 1.2;
  }

  .village-kanji {
    font-size: 1.05rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    color: #fda4af;
    text-shadow: 0 0 10px rgba(244, 63, 94, 0.6);
  }

  .village-name {
    font-family: 'Cinzel', 'Chakra Petch', ui-monospace, sans-serif;
    font-size: 0.92rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    color: #f8fafc;
  }

  .village-tag {
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    padding: 1px 6px;
    border-radius: 9999px;
    background: rgba(244, 63, 94, 0.2);
    color: #f43f5e;
    border: 1px solid rgba(244, 63, 94, 0.4);
    text-transform: uppercase;
  }

  .village-subtitle {
    margin: 0;
    font-size: 0.72rem;
    color: #94a3b8;
    letter-spacing: 0.02em;
  }

  .village-portal-actions {
    pointer-events: auto;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .village-action-btn {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    padding: 8px 14px;
    border-radius: 9999px;
    font-size: 0.8rem;
    font-weight: 600;
    cursor: pointer;
    text-decoration: none;
    transition: all 0.18s ease;
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    user-select: none;
  }

  .open-tab-btn {
    border: 1px solid rgba(255, 255, 255, 0.18);
    background: rgba(15, 23, 42, 0.75);
    color: #e2e8f0;
  }

  .open-tab-btn:hover {
    background: rgba(30, 41, 59, 0.95);
    border-color: rgba(244, 63, 94, 0.4);
    color: #fff;
    transform: translateY(-1px);
    box-shadow: 0 4px 14px rgba(0, 0, 0, 0.35);
  }

  .fullscreen-btn {
    border: 1px solid rgba(255, 255, 255, 0.18);
    background: rgba(15, 23, 42, 0.75);
    color: #e2e8f0;
    padding: 8px 10px;
  }

  .fullscreen-btn:hover {
    background: rgba(30, 41, 59, 0.95);
    color: #fff;
    transform: translateY(-1px);
  }

  .close-btn {
    border: 1px solid rgba(244, 63, 94, 0.45);
    background: rgba(225, 29, 72, 0.82);
    color: #fff;
    box-shadow: 0 4px 16px rgba(225, 29, 72, 0.3);
  }

  .close-btn:hover {
    background: rgba(244, 63, 94, 0.95);
    transform: translateY(-1px) scale(1.02);
    box-shadow: 0 6px 20px rgba(244, 63, 94, 0.45);
  }

  @media (max-width: 640px) {
    .village-portal-chrome {
      padding: 10px 12px;
    }
    .village-subtitle,
    .village-tag,
    .close-label {
      display: none;
    }
    .village-action-btn {
      padding: 7px 10px;
    }
  }
</style>

<script>
  const backdrop = document.getElementById('village-portal-backdrop') as HTMLDivElement | null;
  const iframe = document.getElementById('village-portal-iframe') as HTMLIFrameElement | null;
  const fsBtn = document.getElementById('village-fullscreen-btn') as HTMLButtonElement | null;
  const closeBtn = document.getElementById('village-portal-close') as HTMLButtonElement | null;

  const openTabBtn = document.querySelector('.village-portal-actions .open-tab-btn');

  const VILLAGE_URL = 'https://yorimichi-world.vercel.app/';

  function trackClick(source = '2d') {
    try {
      const payload = JSON.stringify({
        targetType: 'monument',
        targetId: 'yorimichi_village',
        source,
      });
      const blob = new Blob([payload], { type: 'application/json' });
      if (navigator.sendBeacon) {
        navigator.sendBeacon('/api/analytics/click', blob);
      } else {
        fetch('/api/analytics/click', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: payload,
          keepalive: true,
        }).catch(() => {});
      }
    } catch (_) {}
  }

  function openVillageModal(source = '2d') {
    if (!backdrop || !iframe) return;
    trackClick(source);
    if (iframe.src !== VILLAGE_URL) {
      iframe.src = VILLAGE_URL;
    }
    backdrop.classList.add('open');
    backdrop.setAttribute('aria-hidden', 'false');
    document.body.style.overflow = 'hidden';
    iframe.focus();
  }

  function closeVillageModal() {
    if (!backdrop || !iframe) return;
    backdrop.classList.remove('open');
    backdrop.setAttribute('aria-hidden', 'true');
    // Unload iframe to stop audio and WebGL cycles in background
    iframe.src = 'about:blank';
    document.body.style.overflow = '';
  }

  function toggleFullscreen() {
    if (!backdrop) return;
    if (!document.fullscreenElement) {
      backdrop.requestFullscreen?.().catch(() => {});
    } else {
      document.exitFullscreen?.().catch(() => {});
    }
  }

  closeBtn?.addEventListener('click', closeVillageModal);
  fsBtn?.addEventListener('click', toggleFullscreen);
  openTabBtn?.addEventListener('click', () => trackClick('new_tab'));

  window.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && backdrop?.classList.contains('open')) {
      closeVillageModal();
    }
  });

  (window as any).openVillageModal = openVillageModal;
  (window as any).closeVillageModal = closeVillageModal;
  (window as any).loadVillageWorld = openVillageModal;
</script>

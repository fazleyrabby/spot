# Web app env (Astro/Vite reads this from apps/web/.env)
# Point writes at the authoritative API server.
# Leave empty to fall back to direct Supabase mode (dev only — insecure in prod).
PUBLIC_API_BASE=http://localhost:5050

# Optional — defaults are hardcoded for the spot project, override if needed
# PUBLIC_SUPABASE_URL=https://koqodifauvvemouhnjqz.supabase.co
# PUBLIC_SUPABASE_ANON_KEY=...
